<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.samgyeobsal.mapper.MemberMapper">

    <select id="findMemberByEmail" resultType="member">
        select name, password, email, role, loginType
        from member
        where email = #{email} and loginType= #{loginType}
    </select>

    <insert id="insertMember">
        insert into member (email, name, password, role, logintype)
        values (#{email}, #{name}, #{password}, #{role}, #{type})
    </insert>
</mapper>